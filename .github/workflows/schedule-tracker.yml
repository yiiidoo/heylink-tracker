name: Heylink Tracker Cron

on:
  schedule:
    # Her 15 dakikada bir çalıştır
    - cron: '*/15 * * * *'
  workflow_dispatch: # Manuel çalıştırma için

jobs:
  track-heylinks:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Setup Chrome
      uses: browser-actions/setup-chrome@latest

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libnss3 libatk-bridge2.0-0 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxrandr2 libgbm1 libxss1 libasound2t64 libgtk-3-0 libx11-xcb1 libxcb-dri3-0 libxcb-dri3-0 libxss1 libasound2t64

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        playwright install --with-deps chromium

    - name: Run Heylink Tracker
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        python github_actions_tracker.py
      timeout-minutes: 20
